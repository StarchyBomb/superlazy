interface:
  display_name: "Superlazy Worktree"
  short_description: "Silent isolated workspace setup"
  default_prompt: "Use $superlazy-worktree before starting isolated feature work."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Superlazy Debug"
  short_description: "Silent root-cause debugging"
  default_prompt: "Use $superlazy-debug to find and fix the root cause of this failure."
policy:
  allow_implicit_invocation: true

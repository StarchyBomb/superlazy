interface:
  display_name: "Superlazy MCP"
  short_description: "Minimal-token MCP builder"
  default_prompt: "Use $superlazy-mcp to build or modify this MCP server."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Superlazy Subagents"
  short_description: "Silent subagent plan execution"
  default_prompt: "Use $superlazy-subagents to execute independent plan tasks with subagents."
policy:
  allow_implicit_invocation: true
